#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    int a,b,c,total,first,second;
    scanf("%d",&a);
    scanf("%d",&b);
    scanf("%d",&c);
    first=a*b;
    second=a*c;
    total=first-second-100;
    printf("%d",total);
    return 0;
    }
